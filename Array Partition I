class Solution {
public:
    int arrayPairSum(vector<int>& nums) {
        int n=0;
        sort(nums.begin(), nums.end());
        
        for(int i=0;i<nums.size()-1;i=i+2)
        {
            n = n+nums[i];
        }
        return n;
    }
};
