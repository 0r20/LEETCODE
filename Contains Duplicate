class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        bool fl = false;
        
        if(nums.size() == 0)
        {
            return fl;
        }
        sort(nums.begin(),nums.end());
        
        int i=0;
        while(i<nums.size()-1)
        {
            if (nums[i]-)
        }
        
        return fl;
    }
};
